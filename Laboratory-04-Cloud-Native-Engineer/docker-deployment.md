# Docker Deployment & Container Lifecycle

## 1. Environment Status Verification
To verify that the Docker CLI and daemon were installed and running properly on the KillerCoda environment, the following verification commands were executed:

```bash
docker --version
docker info
